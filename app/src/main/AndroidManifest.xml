<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.mayankjainraipur.zendrive">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ZenDriveApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZenDrive">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".AddVehicleActivity" />
        <activity android:name=".VehicleDetailActivity" />
        <activity android:name=".AddEventActivity" />
        <activity android:name=".EventDetailActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".AddDocumentActivity" />
        <activity android:name=".AddReminderActivity" />
        <activity android:name=".ServiceScheduleActivity" />
        <activity
            android:name=".AppLockActivity"
            android:theme="@style/Theme.ZenDrive" />

        <receiver
            android:name=".ReminderActionReceiver"
            android:exported="false" />

        <!-- Hands a viewer app read access to one document copy, for one intent only. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>